/**
 * Phase 3.5 ships the stable-identity mastery path implementation-complete but
 * inert. Enabling it is intentionally a local, one-line release decision.
 */
export const FEATURE_FLAGS = Object.freeze({
  contrastMasteryStore: false,
});
